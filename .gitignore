node_modules
.next
out
.env
.env.local
/data
/public/media/**/*.orig.*
*.tsbuildinfo
next-env.d.ts

# harvested source renders are regenerated by `npm run ingest`
/docs/source/pdf-pages
/content/*.pdf

# OCR language data: 8.6 MB of binaries that tesseract fetches on demand
*.traineddata

# runtime uploads from the admin, not source
/storage

# scratch output from the review harnesses
/.tmpimg
/tmp-*
/_*.mjs

# ảnh thô bóc từ PDF, tái tạo bằng node _harvest.mjs
/_harvest
